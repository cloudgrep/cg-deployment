apiVersion: v1
kind: Service
metadata:
  name: ingestor
  labels:
    app: ingestor
spec:
  type: ClusterIP
  selector:
    app: ingestor
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
