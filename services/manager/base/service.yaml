apiVersion: v1
kind: Service
metadata:
  name: manager
  labels:
    app: manager
spec:
  type: ClusterIP
  selector:
    app: manager
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
